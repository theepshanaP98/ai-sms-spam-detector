# Submission Checklist

## Required by the assignment

- [x] Clearly defined real-world AI use case
- [x] AI/ML component implemented
- [x] Complete web application
- [x] REST API endpoint
- [x] Cloud deployment configuration
- [x] Dockerfile included as deployment fallback
- [ ] Push complete code to a **public Git repository**
- [ ] Deploy the application to Render and verify the public URL
- [ ] Replace `<YOUR NAME>` and `<YOUR STUDENT ID>` in README.md
- [ ] Add the public GitHub repository URL to the final submission
- [ ] Add the deployed Render URL to the final submission
- [ ] Run `python scripts/train_model.py` and retain `models/metrics.json`
- [ ] Run `pytest -q` and confirm tests pass
- [ ] Test the live `/`, `/docs`, and `/api/health` endpoints

## Suggested screenshots for evidence

1. Web UI showing a ham prediction
2. Web UI showing a spam prediction
3. Swagger `/docs` page
4. Render deployment status page
5. GitHub repository root showing source files and README
6. Terminal output from `pytest -q`
7. Training output or `models/metrics.json`
